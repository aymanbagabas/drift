# drift ANSI theme: uses your terminal's own 16 ANSI colors (plus the
# terminal default fg/bg), so it matches whatever palette your terminal
# defines. 256-color terminals can also reference indices 0-255 directly.
#
# Copy this to ~/.config/drift/config.toml and tweak to taste, or pass it
# with --config. Color tokens accept ANSI names (black, red, green, yellow,
# blue, magenta, cyan, white and their bright* variants), a 0-255 index, a
# literal `#rrggbb`, or `default`/`none`/`-` for the terminal default.

theme = "ansi"
syntax = false           # 16 ANSI colors collide with syntax hues; keep text plain
intraline = false        # too few colors to emphasize word changes cleanly
line-numbers = true
tab-width = 4

[colors]
add         = "green"
remove      = "red"
context     = "default"      # terminal default foreground
header      = "blue"
line-number = "brightblack"
primary     = "magenta"      # logo badge background
secondary   = "blue"         # help badge
foreground  = "default"
background  = "default"      # terminal default background
muted       = "brightblack"
surface     = "brightblack"  # status bar / chip background
cursor      = "brightblack"  # current-line highlight
add-line    = "default"      # keep terminal bg; emphasis carries the change
remove-line = "default"
header-line = "brightblack"  # spanning band behind a hunk header
add-emph    = "green"
remove-emph = "red"

# Component styles: `fg bg attrs...`. Attrs: bold, faint/dim, italic,
# underline, strikethrough, blink, reverse, conceal.
[styles]
add                = "add"
remove             = "remove"
context            = "context"
header             = "header bold"
line-number        = "line-number"
statusbar          = "foreground surface"
statusbar-logo     = "background primary bold"
statusbar-filename = "foreground surface bold"
statusbar-add      = "add surface"
statusbar-remove   = "remove surface"
statusbar-flags    = "muted surface"
statusbar-stats    = "foreground surface"
statusbar-watch    = "background add bold"
statusbar-help     = "background secondary bold"
help-key           = "muted bold"
help-desc          = "default"
dialog             = "foreground background"
dialog-border      = "surface"
sidebar-border     = "surface"
